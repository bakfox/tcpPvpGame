syntax = "proto3";

package gameNotification;

// 게임 시작 메세지!
message Start {
  string gameId = 1;
  int64 timestamp = 2;
}

// 위치 변경 메세지!
message LocationUpdate {
  repeated UserLocation users = 1;

  message UserLocation {
    string id = 1;
    float x = 2;
    float y = 3;
  }
}

